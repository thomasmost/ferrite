[package]
name = "hello"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["staticlib"]

[dependencies]
ferrite = { path = "../../crates/ferrite" }

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
codegen-units = 1
